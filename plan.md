# 発表練習デスクトップアプリ 実装計画

## 1. 目的

macOS上で発表開始と同時にマイク音声と指定画面を監視し、画面下部の複数審査員がリアルタイムに反応する常駐アプリを作る。

リアルタイムコメントは次の二系統を同時に動かす。

- 高速レーン: 音量、無音、話速、フィラー、スライド滞在時間などをルールで判定する。
- 理解レーン: 直近の文字起こし、スライドOCR、発表設定をLLMへ渡し、内容に即した短いコメント候補を作る。

終了後はセッション全体を根拠付きで採点し、改善案を提示する。

## 2. MVPの対象

- macOS 14以降、Apple Siliconを第一対象とする。
- 審査員は `tempo`、`clarity`、`slide`、`audience` の4人。
- 透明・常に最前面・クリック透過可能なオーバーレイを表示する。
- マイク音声、画面変化、部分文字起こし、LLMコメントを統合する。
- LLMが利用できなくてもルール判定とUIは動作し続ける。
- 初期設定では発表動画全体を保存せず、イベント、文字起こし、特徴量、変更時の縮小画像だけを保存する。

## 3. 非対象

- Windows対応
- カメラによる姿勢・表情解析
- 発表中の長文アドバイス
- キャラクターや素材による既存ゲーム作品の直接的な模倣

## 4. 技術構成

- 言語: Swift 6
- UI: SwiftUI + AppKit
- 2D演出: SpriteKit
- マイク: AVAudioEngine
- 画面・システム音声: ScreenCaptureKit
- OCR: Vision
- 文字起こし: 初期実装は抽象化された `TranscriptionProviding`。後続で whisper.cpp/Core MLアダプターを追加する。
- LLM: 抽象化された `CommentGenerating`。HTTP実装と疑似実装を分離する。
- 永続化: MVPではJSONL。スキーマ安定後にSQLiteを追加する。
- ビルド・テスト: Swift Package Manager

## 5. モジュール構成

```text
Package.swift
Sources/
  PresentationContracts/     # 全ラインが共有するデータ契約
  PresentationCapture/       # 音声・画面・セッション入力
  PresentationFeedback/      # ルール、LLM候補、演出調停、採点
  PresentationOverlay/       # 審査員、吹き出し、オーバーレイ
  PresentationApp/           # composition root / macOSアプリ
Tests/
  PresentationContractsTests/
  PresentationCaptureTests/
  PresentationFeedbackTests/
  PresentationOverlayTests/
Fixtures/
  Sessions/                   # UI/AIを独立開発するJSONL
Assets/
  Judges/                     # キャラクター定義とアニメーション情報
docs/
  event-contract.md
  comment-policy.md
```

## 6. 共有イベント契約

すべてのモジュール間通信は `PresentationEvent` を使う。UIは実データとFixtureを区別しない。

主要イベント:

- `sessionStarted` / `sessionStopped`
- `audioMetric`
- `speechPartial` / `speechFinal`
- `slideChanged`
- `ruleCommentCandidate`
- `llmCommentCandidate`
- `judgeReaction`
- `timerUpdated`
- `scoreUpdated`

コメント候補は最低限、次を持つ。

- 審査員ID
- 表示文字列
- 感情・アニメーション種別
- 優先度
- 信頼度
- 根拠となる文字列またはイベントID
- 有効期限
- `rule` または `llm` の生成元

## 7. リアルタイム処理

### 高速レーン

- 音声特徴抽出: 10〜20ms単位
- 無音・音量反応: 200ms以内
- 画面差分検出: 200〜300ms単位
- OCR: 画面変更検出時だけ実行
- ルール判定は同期的で決定的にする。

### LLM理解レーン

LLMへの入力:

- 直近20〜30秒の文字起こし
- 現在スライドのOCR
- 発表タイトル、目的、対象者
- 現在セクションと残り時間
- 直近30秒に表示したコメント
- 4人の審査員ペルソナ

呼び出し条件:

- 文が確定したとき
- スライドが切り替わったとき
- 発話継続中の2〜4秒ハートビート

高速化条件:

- 1回の呼び出しで最大3候補を生成する。
- 出力は短い構造化JSONに限定する。
- 同時実行数は原則1。
- 2.5秒を超えた要求や文脈が古くなった結果は破棄する。
- LLMを待つ間も高速レーンとアニメーションを止めない。

### 演出ディレクター

LLMやルールはUIを直接操作しない。`FeedbackDirector` が候補を調停し `judgeReaction` を発行する。

優先順位:

1. 発表継続に必要な警告
2. 内容に具体的に反応するLLMコメント
3. 時間・構成に関する指摘
4. コミカルな小ネタ

制約:

- 原則として同時表示は1件。
- 審査員ごとに6〜12秒のクールダウンを持つ。
- 同一意味の連投を抑制する。
- 吹き出しは原則18〜28文字、最大40文字。
- 有効期限切れの候補は表示しない。

## 8. UIデータの独立開発

UI担当はキャプチャやLLMの完成を待たず、JSONLのFixtureを再生して実装する。

最初に用意するFixture:

- 通常進行
- 話速過多
- 長い無音
- 情報量過多のスライド
- ルールとLLM候補の競合
- LLM遅延・タイムアウト
- コメント集中時の優先順位

キャラクターの見た目と挙動はデータ化する。

- `judge.json`: ID、表示名、テーマ色、吹き出し位置
- `animations.json`: idle、happy、confused、panic、sleepなどの状態
- Sprite Atlasまたは仮のベクター描画
- 効果音のIDと再生タイミング

## 9. 3人の担当

### 開発者A: キャプチャ・基盤・統合

- Swift PackageとmacOSアプリ骨格
- 共有イベント契約
- セッションイベントバス
- AVAudioEngine入力と軽量音声特徴抽出
- ScreenCaptureKitアダプター
- JSONL記録・再生
- 最終統合と性能計測

### 開発者B: AI・評価

- ルールエンジン
- `CommentGenerating` プロトコル
- LLM向け入力コンテキストと構造化出力
- 候補の期限切れ、キャンセル、重複排除
- `FeedbackDirector`
- 最終採点の基礎モデル

### 開発者C: UI・演出

- 透明オーバーレイ
- SpriteKit審査員シーン
- 吹き出し
- アニメーション状態機械
- UI用Fixtureプレイヤー
- キャラクターマニフェスト

各担当は自分のモジュールとテストを主に編集する。`PresentationContracts` の変更は全員で確認する。

## 10. 実装順

### 現在地

共有イベント契約、ルールエンジン、疑似LLM、フィードバック調停、JSONL保存・再生、採点ロジック、オーバーレイUIに加え、macOSアプリ基盤とメニューバー操作まで実装済み。

実マイク入力はセッションイベントバス、ルール判定、JSONL保存、審査員UIへ接続済み。Apple Development署名によるマイク・画面収録の実機許可確認は開発環境側の作業として残る。次の主作業はScreenCaptureKit、文字起こし、実LLM、終了後レポートである。

### タスクの依頼方法

未完了タスクには一意なIDを付ける。担当者にはIDを指定して依頼し、PRタイトルまたは本文にもIDを記載する。

依頼例:

```text
APP-001〜APP-003を担当してください。完了条件はM1の1〜3です。
AI-003、AI-005〜AI-008を担当してください。実LLM資格情報はKeychainから取得してください。
SCREEN-001〜SCREEN-005を担当してください。古いフレームを蓄積しないでください。
```

| 接頭辞 | 領域 | 推奨担当 |
|---|---|---|
| `APP` | macOSアプリ統合、常駐、権限、ライフサイクル | 開発者A＋C |
| `AI` | 文字起こし、実LLM、遅延制御 | 開発者B |
| `SCREEN` | 画面取得、差分、OCR | 開発者A |
| `REPORT` | 集計、最終評価、履歴UI | 開発者B＋C |
| `SHIP` | 品質計測、署名、配布 | 開発者A |
| `WIN` | Windows対応と共通コア | 3人で設計後に分担 |

主な依存関係:

- M1は `APP-001` → `APP-002`・`APP-003` → `APP-005`・`APP-008` の順で進める。
- ライブ練習は `APP-006`・`APP-007` に、`AI-002`・`AI-003` と `SCREEN-002` が合流する。
- `REPORT-001`〜`REPORT-004` は文字起こし・画面イベントの形式確定後に進める。
- `SHIP`はM2完了後、`WIN`はmacOS MVP完了後を基本とする。

### フェーズ0: 契約と技術検証

- [x] Swift Packageを作成する。
- [x] イベント契約を実装する。
- [x] JSONL Fixtureを作成する。
- [x] 各モジュールの単体テストを起動できるようにする。
- [x] 透明ウィンドウとSpriteKit描画を検証する。
- [x] マイク入力とRMS計測を検証する。

### フェーズ1: 動く縦切り

- [x] マイク音量イベントを発行する。
- [x] ルールがコメント候補を生成する。
- [x] ディレクターが `judgeReaction` を選ぶ。
- [x] UIモジュールがFixtureから審査員と吹き出しを表示する。
- [x] セッションをJSONLへ保存・再生する。
- [x] CLIデモでルール候補、疑似LLM候補、採点、JSONL保存を統合する。

### フェーズ2: 起動できるmacOSアプリ

- [x] `[APP-001]` `PresentationApp` をAppKit/SwiftUIアプリとして起動する。
- [x] `[APP-002]` `swift run PresentationApp --ui-demo` で透明オーバーレイを表示する。
- [x] `[APP-003]` `FixtureTimeline` を `OverlayViewModel` へ接続する。
- [x] `[APP-004]` メニューバー常駐、練習開始、停止、終了を実装する。
- [x] `[APP-005]` 透明、常に最前面、クリック透過を実機で確認する。
- [x] `[APP-006]` マイクと画面収録の権限状態・案内画面を実装する。
- [x] `[APP-007]` セッションイベントバスからUIまでのライブ経路を接続する。
- [x] `[APP-008]` アプリ終了時にキャプチャ、タスク、ファイルを安全に閉じる。

`APP-006`の実装は完了。Apple Development証明書を作成した環境で、TCCへの登録と許可後の再起動を最終確認する。

### フェーズ3: 実文字起こしとLLMコメント

- [x] 部分文字起こしをイベント契約へ追加する。
- [x] 疑似LLMの構造化レスポンスを実装する。
- [x] キャンセル、期限切れ、古い応答の破棄を実装する。
- [x] ルールとLLMの競合をテストする。
- [ ] `[AI-001]` `TranscriptionProviding` プロトコルを実装する。
- [ ] `[AI-002]` whisper.cpp/Core MLを接続し、部分・確定文字起こしを発行する。
- [ ] `[AI-003]` 実LLM向けHTTPアダプターと資格情報設定を実装する。
- [ ] `[AI-004]` APIキーをmacOS Keychainへ保存する。
- [ ] `[AI-005]` 直近20〜30秒の文字起こし、スライドOCR、残り時間からLLMコンテキストを作る。
- [ ] `[AI-006]` 1回のLLM呼び出しから最大3件の審査員候補を復号する。
- [ ] `[AI-007]` 2.5秒タイムアウト、同時実行1件、古い要求のキャンセルをライブ経路で確認する。
- [ ] `[AI-008]` ネットワーク断・LLM失敗時にルールレーンだけで継続する。
- [ ] `[AI-009]` 実機でLLMコメントのp50/p95レイテンシを計測する。

### フェーズ4: 画面解析

- [x] ScreenCaptureKitから利用可能なディスプレイを列挙する。
- [ ] `[SCREEN-001]` 監視するディスプレイまたはウィンドウを選択できるようにする。
- [ ] `[SCREEN-002]` ScreenCaptureKitのフレームストリームを開始・停止する。
- [ ] `[SCREEN-003]` 画面変更検出と自アプリ除外を実装する。
- [ ] `[SCREEN-004]` 古いフレームを捨てるbounded queueを実装する。
- [ ] `[SCREEN-005]` 変更検出時だけVision OCRを実行する。
- [ ] `[SCREEN-006]` スライドID、文字量、滞在時間をイベント化する。
- [ ] `[SCREEN-007]` 高密度スライド、長時間滞在、切り替え過多のルールをライブ入力で確認する。

### フェーズ5: 終了後評価と履歴

- [x] 6カテゴリ・100点満点の決定的な基礎採点を実装する。
- [ ] `[REPORT-001]` セッション全体から採点用メトリクスを集計する。
- [ ] `[REPORT-002]` 最終スコアと根拠タイムラインを作る。
- [ ] `[REPORT-003]` 終了後評価用LLMで良かった点、改善点、次回課題を生成する。
- [ ] `[REPORT-004]` 各指摘に文字起こしまたはスライドのタイムスタンプを付ける。
- [ ] `[REPORT-005]` 結果画面を実装する。
- [ ] `[REPORT-006]` 過去セッション一覧と詳細表示を実装する。
- [ ] `[REPORT-007]` JSONまたはMarkdownでレポートを書き出せるようにする。

### フェーズ6: 品質・配布

- [ ] `[SHIP-001]` アプリアイコン、名称、権限説明文を追加する。
- [ ] `[SHIP-002]` `.app`としてビルドできるXcodeプロジェクトまたは生成手順を整備する。
- [ ] `[SHIP-003]` マイク・画面収録・ネットワークのentitlementを整理する。
- [ ] `[SHIP-004]` ReleaseビルドでCPU、メモリ、フレームレート、反応時間を計測する。
- [ ] `[SHIP-005]` 長時間セッション、スリープ復帰、デバイス切り替えをテストする。
- [ ] `[SHIP-006]` コード署名とNotarizationを設定する。
- [ ] `[SHIP-007]` クラッシュログと診断ログの方針を決める。
- [ ] `[SHIP-008]` 自動アップデート方式を決める。

### フェーズ7: Windows対応の準備

macOS MVP完了後に着手する。ただし、共有契約の互換性は早い段階から維持する。

- [ ] `[WIN-001]` Swift固有のassociated-value JSONではなく、`version`、`type`、`payload`を持つ言語非依存スキーマへ変更する。
- [ ] `[WIN-002]` `AudioCapturing`、`ScreenCapturing`、`TranscriptionProviding`、`OverlayRendering`の境界を定義する。
- [ ] `[WIN-003]` 共有コアをSwiftのまま維持するかRustへ移すかを技術検証する。
- [ ] `[WIN-004]` Windows音声取得用WASAPIアダプターを設計する。
- [ ] `[WIN-005]` Windows.Graphics.Captureアダプターを設計する。
- [ ] `[WIN-006]` WinUI 3/Win2Dの透明・最前面オーバーレイを技術検証する。
- [ ] `[WIN-007]` キャラクターマニフェスト、Fixture、効果音を両OSで共有する。

## 11. 初期性能目標

- ルール反応: 入力から200ms以内
- LLMコメント: p50 1.5秒以内、p95 2.5秒以内を目標
- オーバーレイ: 60fpsを維持
- キャプチャ処理中もUIメインスレッドをブロックしない
- LLM停止・ネットワーク断でも高速レーンが継続する
- 古い画面フレームはキューに溜めず破棄する

## 12. マイルストーン完了条件

### M0: CLI縦切り（完了）

1. CLIデモを開始する。
2. Fixtureまたはマイク入力からイベントが流れる。
3. ルールコメントと疑似LLMコメントが競合する。
4. ディレクターが1件を選択する。
5. UIまたはCLI表示に審査員、感情、吹き出しが現れる。
6. 同じイベント列をJSONLに保存し再生できる。
7. `swift test` が成功する。

### M1: UIデモ

1. `swift run PresentationApp --ui-demo` で透明オーバーレイが開く。
2. 4人の審査員が画面下部に表示される。
3. Fixtureのルール・LLMコメントが順番に吹き出しとして表示される。
4. UIを操作せず下の発表画面をクリックできる。
5. デモ終了時にウィンドウと再生タスクが安全に終了する。

### M2: ライブ練習MVP

1. メニューバーから発表練習を開始・停止できる。
2. マイク、画面、文字起こし、ルール、実LLMが同時に動作する。
3. ルールコメントは入力から200ms以内を目標に表示される。
4. LLMコメントはp95 2.5秒以内を目標に表示される。
5. LLM失敗時も練習を継続できる。
6. 終了後に100点評価、根拠、改善案が表示される。
7. セッションを履歴から再表示できる。

### M3: 配布可能版

1. 署名済み`.app`を別のMacへインストールできる。
2. 初回権限案内から練習開始まで迷わず進める。
3. 30分以上のセッションでクラッシュ、音声停止、著しいメモリ増加がない。
4. Releaseビルドの性能目標を満たす。
